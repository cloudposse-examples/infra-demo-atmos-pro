components:
  terraform:
    cluster:
      settings:
        depends_on:
          1:
            component: load-balancer
      vars:
        enabled: true
        name: cluster
        vpc_id: '{{ (atmos.Component "load-balancer").outputs.vpc_id }}'
        lb_id: '{{ (atmos.Component "load-balancer").outputs.lb_id }}'




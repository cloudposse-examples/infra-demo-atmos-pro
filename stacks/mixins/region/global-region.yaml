# This file is for vars only; do not import components here
# For more information, see "Mixins and Imports with Atmos" in the [baseline documentation](../docs/setup/00-baseline.yaml)
vars:
  region: us-east-1
  environment: gbl

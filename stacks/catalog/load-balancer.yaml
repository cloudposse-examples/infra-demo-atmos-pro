components:
  terraform:
    load-balancer:
      settings:
        depends_on:
          1:
            component: network
      vars:
        enabled: true
        name: "lb"
        vpc_id: '{{ (atmos.Component "vpc").outputs.vpc_id }}'

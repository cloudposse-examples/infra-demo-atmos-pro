import:
  - orgs/ex1/plat/dev/_defaults
  - mixins/region/us-east-2

  - catalog/network
  - catalog/load-balancer
  - catalog/cluster

components:
  terraform:
    network:
      vars:
        name: network1

    load-balancer:
      vars:
        name: lb5

    cluster:
      vars:
        name: cluster1
